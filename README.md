# Vocab App

Language Vocabulary Learning Web UI

Commands:

- Install: npm install
- Run dev: npm run dev
- Test: npm test
- Test with coverage: npm run test:coverage
- Build: npm run build

This project is a small React + Vite + TypeScript app with Tailwind CSS and Vitest tests. The data is in-memory and persisted to localStorage for quick local progress tracking.
